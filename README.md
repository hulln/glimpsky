
# Bluesky Viewer

Really simple viewer for Bluesky posts and likes.

Start a server in this folder:
```sh
python3 -m http.server 5173
```

Vibecoded. Hehe.

If you add new files to your repo, save them to GitHub with:
```sh
git add .
git commit -m "your message"
git push
```
